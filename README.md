# 컴파일 및 기본 실행 명령어

```Bash
g++ 파일이름.cpp -o main
```

- 의미 : 
  - g++ : C++ 컴파일러 실행

  - 파일이름.cpp : 컴파일할 C++ 파일

  - -o main : 실행 파일 이름을 main으로 생성

  - **.cpp 파일이 변경 될 때 마다 실행해야하는 명령어**

```Bash
./main
```

- 파일 실행